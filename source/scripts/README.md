# Folder for scripts

You can write simple scripts in `index.js`, or create a modular system for several scripts:

```shell
└── source/
    └── scripts/
        ├── index.js
        ├── modal.js
        └── nav.js
```
